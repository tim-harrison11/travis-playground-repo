os: linux
language: python
dist: focal
stage: Instal addons on linux
group: edge
name: Premium VM builds
python: '3.9'
arch: amd64
vm:
  size: large
script:
- |-
  df -h
  lspci
  echo "Going for sleep"
  for ((i=1; i<=1; i++)); do
    echo "Elapsed minutes: $i"
    sleep 60s
  done

